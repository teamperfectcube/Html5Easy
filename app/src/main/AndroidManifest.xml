<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.itsgaurav.html5easy">

    <!--permission for internet-->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
    <!--MainActivity -->
    <activity android:name=".activities.MainActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

    <!--hTMLIntroductionActivity-->
    <activity
        android:name=".activities.HTMLIntroductionActivity"
        android:label="HTML5 Introduction">
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".activities.MainActivity" />
    </activity>

    <!--HTMLElementsActivity-->
    <activity
        android:name=".activities.HTMLElementsActivity"
        android:label="HTML Elements">
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".activities.MainActivity" />
    </activity>

    <!--HTMLVideoActivity -->
    <activity
        android:name=".activities.HTMLVideoActivity"
        android:label="HTML5 Video">
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".activities.MainActivity" />
    </activity>

    <!--HTMLAudioActivity -->
    <activity
        android:name=".activities.HTMLAudioActivity"
        android:label="HTML5 Audio">
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".activities.MainActivity" />
    </activity>

    <!--HTMLDragAndDropActivity-->
    <activity
        android:name=".activities.HTMLDragAndDropActivity"
        android:label="HTML5 Drag and Drop">
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".activities.MainActivity" />
    </activity>

    <!--HTMLCanvasActivity -->
    <activity
        android:name=".activities.HTMLCanvasActivity"
        android:label="HTML5 Canvas">
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".activities.MainActivity" />
    </activity>

    <!--HTMLSvgActivity -->
    <activity
        android:name=".activities.HTMLSvgActivity"
        android:label="HTML SVG">
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".activities.MainActivity" />
    </activity>

    <!--HTMLGeoLocationActivity -->
    <activity
        android:name=".activities.HTMLGeoLocationActivity"
        android:label="HTML5 GoLocation">
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".activities.MainActivity" />
    </activity>

    <!--HTMLWebStorageActivity -->
    <activity
        android:name=".activities.HTMLWebStorageActivity"
        android:label="HTML5 Web Storage">
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".activities.MainActivity" />
    </activity>

    <!--HTMLWebWorkersActivity -->
    <activity
        android:name=".activities.HTMLWebWorkersActivity"
        android:label="HTML5 Web Workers">
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".activities.MainActivity" />
    </activity>

    <!--HTMLSseActivity -->
    <activity
        android:name=".activities.HTMLSseActivity"
        android:label="HTML5 SSE">
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".activities.MainActivity" />
    </activity>

    </application>
</manifest>
